OUTPUT 2500AD
.ORG 2000
DATA SEGMENT
STRING1 EQU "HELLO"
STRING2 EQU "HELLO"
RESULT DB 00H
COUNT DB 05H
DATA ENDS
CODE SEGMENT
        ASSUME CS:CODE,DS:DATA,ES:DATA
        MOV AX,DATA
        MOV DS,AX
        MOV ES,AX
        MOV SI,OFFSET STRING1
        LEA DI,STRING2
        CLD
L2:     CMPSB
        JZ L1
        MOV RESULT
        JMP L3
L1: MOV RESULT,01H
        DEC CL
        JNZ L2
L3:     HLT
CODE ENDs
END
